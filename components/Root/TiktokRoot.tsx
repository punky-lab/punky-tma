import { PropsWithChildren } from "react";

export function TiktokRoot(props: PropsWithChildren) {
  return (
    <div className="h-screen w-screen flex flex-col items-center justify-center">
      <p>Tiktok is not supported yet</p>
    </div>
  );
}
