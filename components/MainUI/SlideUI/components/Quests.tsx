import React from "react";
import styled from "styled-components";
import { Window, WindowHeader, WindowContent } from "react95";

export default function Quests() {
    return (
        <Window>
            <WindowHeader>Quests</WindowHeader>
        </Window>
    )
}